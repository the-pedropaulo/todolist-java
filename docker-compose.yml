version: '3'

services:
  database:
    image: 'postgres:latest'

    ports:
      - 5432:5432

    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: shoppingjava123 
      POSTGRES_DB: shopping_database